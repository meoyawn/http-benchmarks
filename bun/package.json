{
  "name": "bun-http-benchmark",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "bun build --compile --minify src/server.ts src/sqlite-worker.ts --outfile dist/server"
  },
  "dependencies": {
    "valibot": "1.5.0"
  },
  "devDependencies": {
    "@types/bun": "1.4.2",
    "typescript": "5.9.3"
  }
}
